import 'package:flutter/material.dart';

class ReviewListMenu extends StatelessWidget {
  const ReviewListMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView.builder(itemBuilder: (context, index) {

    },itemCount: 2,);
  }
}
